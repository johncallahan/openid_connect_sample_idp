# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  # protocol
  endpoint:
    help: Protocol Endpoints
    authorization: Authorization Endpoint
    token: Token Endpoint
    userinfo: UserInfo Endpoint
  # per model
  client:
    identifier: Client ID
    secret: Client Secret
    credentials: Credentials
  authorization:
    scope: Requested Scopes
  # per controller
  site:
    title: OpenID
    description: Connect Now!
  top:
    title: Let's create blockchain credentials for you...
    description: You can use this app to create blockchain credentials for your GBAId
  dashboard:
    title: Registered Clients
    help:
      title: Endpoint Info
  clients:
    new:
      title: Register New Client
  authorizations:
    new:
      title: Authorize %{client} ?
      description: After approved, you will be redirected to %{redirect_uri}
  connect:
    fakes:
      new:
        title: Create Credentials
  # others
  help:
    title: Help